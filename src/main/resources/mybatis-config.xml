<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <properties resource="db.properties">
    </properties>
    <settings>
        <setting name="logImpl" value="LOG4J"/>
        <setting name="mapUnderscoreToCamelCase" value="true"/>
    </settings>
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${db.driver}"/>
                <property name="url" value="${db.url}"/>
                <property name="username" value="${db.username}"/>
                <property name="password" value="${db.password}"/>
            </dataSource>
        </environment>
    </environments>
    <mappers>
        <mapper class="DB.mappers.SportMapper"/>
        <mapper class="DB.mappers.PlayerMapper"/>
        <mapper class="DB.mappers.CityMapper"/>
        <mapper class="DB.mappers.CoachMapper"/>
        <mapper class="DB.mappers.CountryMapper"/>
        <mapper class="DB.mappers.MedalsMapper"/>
        <mapper class="DB.mappers.OlympicsMapper"/>
        <mapper class="DB.mappers.TeamMapper"/>
        <mapper class="DB.mappers.CountryStatsMapper"/>
    </mappers>
</configuration>